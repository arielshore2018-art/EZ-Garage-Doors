---
/**
 * Thin urgent strip: short, direct, mobile-friendly.
 * Links to the emergency page + call CTA. No availability promises.
 */
import CallButton from "./CallButton.astro";

interface Props {
  question?: string;
  cta?: string;
}

const {
  question = "Door stuck open, off track, or spring broken?",
  cta = "Call for Fast Local Service",
} = Astro.props;
---

<aside class="emergency-bar">
  <div class="wrap emergency-bar__inner">
    <p class="emergency-bar__q">
      {question}
      <a href="/emergency-garage-door-repair/" class="emergency-bar__link">Emergency repair info →</a>
    </p>
    <CallButton label={cta} fallbackHref="/emergency-garage-door-repair/" track="emergency_call_click" />
  </div>
</aside>

<style>
  .emergency-bar {
    background: var(--ez-ink);
    color: var(--ez-white);
    padding-block: var(--space-4);
  }

  .emergency-bar__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-4);
    flex-wrap: wrap;
  }

  .emergency-bar__q {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: var(--text-lg);
    display: flex;
    align-items: center;
    gap: var(--space-3);
    flex-wrap: wrap;
    margin: 0;
  }

  .emergency-bar__link {
    color: var(--ez-on-ink);
    font-family: var(--font-body);
    font-weight: 400;
    font-size: var(--text-sm);
  }

  .emergency-bar__link:hover {
    color: var(--ez-white);
  }
</style>
