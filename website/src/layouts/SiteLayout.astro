---
/**
 * Full website layout: header with nav, footer with the county index,
 * sticky mobile call bar.
 */
import BaseLayout from "./BaseLayout.astro";
import LogoHeader from "../components/LogoHeader.astro";
import FooterTrustZone from "../components/FooterTrustZone.astro";
import StickyMobileCTA from "../components/StickyMobileCTA.astro";
import type { Crumb } from "../components/Breadcrumbs.astro";

interface Props {
  title: string;
  description: string;
  service?: { name: string; description: string; url: string };
  faqs?: { question: string; answer: string }[];
  breadcrumbs?: Crumb[];
  noindex?: boolean;
}

const props = Astro.props;
---

<BaseLayout {...props}>
  <slot name="head" slot="head" />
  <LogoHeader />
  <main id="main">
    <slot />
  </main>
  <FooterTrustZone />
  <StickyMobileCTA />
</BaseLayout>
